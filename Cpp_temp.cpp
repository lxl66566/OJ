#include <iostream>
#include <vector>
#include <algorithm>
#include <regex>
using namespace std;
typedef long long ll;

int main()
{
	ios::sync_with_stdio(0);
    cin.tie(nullptr);
    
    return 0;
}