#include "template.h"
#include <algorithm>
#include <iostream>

using namespace absx;
using std::cin, std::cout, std::endl;

int main() {
  std::ios::sync_with_stdio(0);
  std::cin.tie(nullptr);

  return 0;
}