// #include "template.h"
#include <iostream>
#include <vector>

// using namespace absx;
using std::cin, std::cout, std::endl, std::vector;

int main() {
  std::ios::sync_with_stdio(0);
  std::cin.tie(nullptr);

  return 0;
}